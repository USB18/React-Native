import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        marginTop: 20,
    },
    text: {
        fontWeight: '500',
        fontSize: 12,
        color: '#353A48',
        lineHeight: 20,
    },
});

export default styles