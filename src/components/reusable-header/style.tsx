import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    buttonContainer: {
        alignSelf: 'flex-end',
        marginTop: 30,
        marginEnd: 30,
    },
});

export default styles